# Fetch recursivo

Ejemplo de fetch recursivo para recorrer grandes bases de datos, apis... o lo que sea. Cabe destacar que la llamada se produce gracias a que fetch devuelve una "promesa". De hecho 
lo suyo seria comprobar ademas __request.status == 200__, ademas de que indice no supere los 5000... pero bueno.
Solo es un ejemplo.